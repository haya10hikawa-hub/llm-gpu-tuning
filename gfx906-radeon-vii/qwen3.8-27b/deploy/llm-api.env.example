# Copy to /etc/llm-api.env and chmod 600. Generate the key with:
#   python3 -c "import secrets;print('sk-local-'+secrets.token_urlsafe(32))"
LLAMA_API_KEY=sk-local-REPLACE_ME
GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1
