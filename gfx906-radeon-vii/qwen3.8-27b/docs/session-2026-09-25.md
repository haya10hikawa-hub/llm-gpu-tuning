# 2026-09-25 セッションログ

GPU は Bonsai-2-27B の評価に数時間占有されていたため、新規最適化案は
GPU 未検証のまま記録する。速度に関する新しい主張は含まない。

## 齟齬: HVV の効果量が記録より大きい

施策4 (`GGML_VK_DISABLE_HOST_VISIBLE_VIDMEM=1`) は本番導入済みで、出力一致も
再確認した。ただし効果量がこのセッションでは記録より大きい。

| | このセッション | findings.md #4 |
|---|---|---|
| baseline (変数なし) | 約10 t/s | 21.77 t/s |
| HVV 有効 | 22〜25 t/s | 26.11 t/s |
| 倍率 | 2.3〜2.5倍 | +20% |

HVV 有効後はほぼ一致するが baseline がこのセッションだけ低い。長時間の
プロセス起動・終了で host-visible ヒープがより断片化した疑いがある。
**規約9により既存の数値は書き換えず、対照付きの再測定を申し送る。**

## decode 内訳 (`GGML_VK_PERF_LOGGER=1`, HVV有効)

1トークン 39.55ms のうち、量子化 matvec が 74.5% (490 GB/s 加重平均、
iq3_s/q3_K が最も低く 287/362 GB/s)。残り 25.5% は小さな演算約1,234回。

うち再帰状態の読み書き (GET_ROWS+CPY) は 4.65% — `rejected.md` の
「クリーン状態 3.3%」に近く、同ファイルが警告する「36.6% の断片化
アーティファクト」ではないことの傍証になる。

## 保留中の試作 4件 (correctness のみ合格、速度未計測)

1. iq4_xs matvec: LUT を byte 単位にして LDS 読み出しと命令数を半減
2. rms_norm: サブグループ和 + レジスタキャッシュ
3. qwen35 グラフ: q/k の L2 norm 統合、matvec+ADD 融合の対象を増やす
4. 再帰状態の恒等コピーをビュー参照に置換 (`rejected.md` の「着手せず」に対応)

`LLAMA_QWEN35_GRAPH_ORIG=1` / `LLAMA_RS_VIEW_DISABLE=1` で元に戻せる。
(3)(4) の見積り (約1.9ms) は測定前の静的見積りで、規約6により保証はない。

## 次のステップ

- 対照付きで HVV 効果を再測定
- 上記4件を実サーバーで速度計測、KLD で正しさを確認
- 効果が出たものだけ `patches/` へ昇格
