# O-01 事前予測 (実行前に記録)

## 特定した機序

`ggml-vulkan.cpp:2672-2692`

    uint32_t rm_kq = 2;                  // 既定 (RDNA, NVIDIA など)
    if (AMD && architecture == AMD_GCN) {
        rm_stdq = 2;
        rm_kq = 4;                       // GCN のみ 2 倍
        rm_stdq_int = 4;
    }
    uint32_t rm_iq = 2 * rm_kq;          // GCN: 8 / 他: 4

rows-per-workgroup が累算器の数を決めるため、これが VGPR 使用量の直接の原因。
gfx906 の実測: I-quant と q6_k が VGPR 256 -> 1 wave/SIMD。

## トレードオフ

行数を減らすと:
  + 累算器が減る -> VGPR 減 -> occupancy 向上 -> レイテンシ隠蔽
  - 活性化ベクトル B の再読み込みが増える -> 帯域を余計に使う

GCN で 4/8 が選ばれているのは意図的なチューニングのはずで、
単純に戻せば速くなるとは限らない。

## 予測 (規約 #6: 実測範囲内)

| 項目 | 予測 |
|---|---|
| rm_kq=2, rm_iq=4 にしたときの VGPR | **256 -> 128~160 (1 wave -> 2 wave)** に下がる |
| decode への影響 | **-5% ~ +20%** (両効果が相殺するため不確実) |

VGPR が下がることは高確度で予測する (累算器が半減するため)。
性能が上がるかは不確実。上がらなければ「現行のチューニングが正しい」と確認できる。

## 判定

1. まず VGPR を確認する (性能測定より速い)
2. VGPR が下がらなければ機序の理解が誤り -> 中断して再診断
3. VGPR が下がったら性能を測る
4. decode が +5% 未満なら O-01 を棄却し、現行チューニングを追認する

## 外れた場合
VGPR が下がらなければ、rows-per-workgroup 以外がレジスタを食っている。
その場合はシェーダ本体 (dequant_funcs.glsl の中間値) を見る必要がある。
